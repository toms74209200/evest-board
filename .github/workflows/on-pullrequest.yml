name: On Pull Request

on:
  pull_request:
    branches:
      - master

jobs:
  build:
    uses: ./.github/workflows/job-build.yml

  test-small:
    needs:
      - build
    uses: ./.github/workflows/job-test-small.yml
